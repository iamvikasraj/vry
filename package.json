{
  "name": "vry-portfolio",
  "version": "1.0.0",
  "description": "A clean, simple portfolio website",
  "main": "index.html",
  "scripts": {
    "dev": "npx live-server --port=3000 --host=0.0.0.0 --open=/",
    "start": "npx live-server --port=3000 --host=0.0.0.0 --open=/",
    "build": "echo 'Static site - no build needed'",
    "deploy": "node scripts/deploy.js deploy",
    "version:create": "node scripts/version.js create",
    "version:list": "node scripts/version.js list",
    "version:current": "node scripts/version.js current"
  },
  "keywords": [
    "portfolio",
    "design",
    "creative"
  ],
  "author": "Vikas Raj",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
